// theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#60E5AD", // Primary color
    },
    secondary: {
      main: "#1467B0", // Secondary color
    },
    thirdonary: {
      main: "#102030", // Example for a custom color
    },
  },
});

export default theme;
